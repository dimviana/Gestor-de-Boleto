# Environment variables for Boleto Manager AI Backend

# MySQL Database Connection
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_database_password
DB_DATABASE=boleto_manager_ai

# Server Configuration
PORT=3001

# JWT Secret for Authentication
# Leave this blank to have the application securely generate one automatically on first startup,
# or provide your own long, random, and secure string.
JWT_SECRET=

# (Optional) Path to Python executable in a virtual environment
# The deploy script sets this automatically. Only needed for manual setups.
PYTHON_PATH=